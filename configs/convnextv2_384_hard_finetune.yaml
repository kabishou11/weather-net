data:
  train_dir:
  train_csv:
  image_root:
  test_dir:
  test_csv:
  image_column:
  label_column:
  val_fraction: 0.2
  folds: 1
  seed: 43
  num_workers: 6
  augment_policy: weather_augmix
  transform_backend: auto
model:
  name: convnextv2_tiny.fcmae_ft_in22k_in1k
  image_size: 384
  pretrained: true
  dropout: 0.1
train:
  epochs: 8
  batch_size: 24
  lr: 0.00008
  weight_decay: 0.0001
  no_weight_decay: true
  layer_decay: 0.85
  label_smoothing: 0.05
  loss_name: class_balanced_focal
  focal_gamma: 1.0
  class_balanced_beta: 0.999
  sampler_mode: sample_weighted
  sample_weight_usage: sampler
  amp: true
  mixup_alpha: 0.1
  cutmix_alpha: 0.15
  ema_decay: 0.999
  output_dir: outputs/convnextv2_384_hard_finetune
infer:
  batch_size: 48
  tta: false
  amp: false
  output_csv: submission_convnextv2_384_hard_finetune.csv
